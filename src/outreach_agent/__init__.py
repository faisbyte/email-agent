"""Deterministic outreach automation.

Python drives the loop. Claude is called for exactly two things: composing an
email body and classifying a reply. Everything else is ordinary code you can
read, test, and reason about.
"""

__version__ = "0.1.0"
